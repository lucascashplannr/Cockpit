node_modules/
dist/
out/
release/
*.tsbuildinfo

# The icon and the icns are committed; the folder they were assembled from
# is an intermediate iconutil already folded into the .icns.
apps/desktop/build/icon.iconset/
.DS_Store

# §15 — the memory is versioned; the journal and sessions are local.
.cockpit/journal.jsonl
.cockpit/sessions/

# The core bundle is a build output of scripts/bundle-core.mjs, rebuilt on every
# package. Committing it would mean a second copy of the core that can disagree
# with packages/core/src.
apps/desktop/resources/
